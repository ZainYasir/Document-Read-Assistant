numpy<2
torch==2.1.0
faiss-cpu==1.7.4

# Use Transformers ≥4.41.0 for Phi‑2 & Sentence‑Transformers compatibility
transformers==4.41.0
sentence-transformers==3.4.1

# Pin Hub to a version that still has cached_download()
huggingface_hub==0.25.2
